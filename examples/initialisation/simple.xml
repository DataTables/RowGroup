<?xml version="1.0" encoding="UTF-8" ?>
<dt-example table-type="html" order="1">

<css lib="datatables rowgroup"/>
<js lib="jquery datatables rowgroup">
<![CDATA[

$(document).ready(function() {
	$('#example').DataTable( {
		order: [[2, 'asc']],
		rowGroup: {
			dataSrc: 2
		}
	} );
} );

]]>
</js>

<title lib="RowGroup">Simple initialisation</title>

<info><![CDATA[

The RowGroup extension for DataTables provides an easy to use row grouping feature for DataTables (the clue is in the name!). It can be configured via the `-init rowGroup` parameter and you will almost always wish to use the `-init rowGroup.dataSrc` option to tell the software what data point in the table's source data to use to get the grouping information.

In this example the _Office_ column (column index 2) is used.

]]></info>

</dt-example>

